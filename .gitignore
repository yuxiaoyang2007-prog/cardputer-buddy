# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/

# Runtime logs
*.log

# macOS cruft
.DS_Store

# venv (we install daemon's venv to ~/.cardputer-daemon/, not in repo)
.venv/
venv/

# Local WiFi credentials for the buddy bundle — never commit real home creds
wifi_secret.py
